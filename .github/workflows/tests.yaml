name: Test keymap options
on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: Nuclear-Squid/zmk/.github/workflows/build-user-config.yml@aekeynox
    if: github.event_name != 'push' || vars.RUN_CI == 1
    with:
      archive_name: test-firmwares
      build_matrix_path: tests.yaml
